# sigzenbi_client

The authoritative docs live next to the code at **`sigzenbi_client/CLAUDE.md`**, with
subsystem detail in `sigzenbi_client/API/gateway/CLAUDE.md`.

Do not add content here — this file exists only so navigation from the app root finds the
real docs. A previous full copy here drifted out of step with the originals, which is worse
than no copy at all.
